(* simple Hello World for OCaml *)

(* define function "main" that has no arguments and returns nothing 
   - indicated with "()" after name *)
let main () = 
	Printf.printf "Hello, OCaml World!\n";
	exit 0;;


(* invoke our main function *)
main();;
